#include "calculator_impl.h"

namespace swu::math {

double CalculatorImpl::add(double a, double b) const {
    return a + b;
}

double CalculatorImpl::multiply(double a, double b) const {
    return a * b;
}

} // namespace swu::math
